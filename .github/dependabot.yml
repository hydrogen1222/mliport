version: 2
updates:
  # Maintain dependencies for GitHub Actions.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 10

  # Python: the mliport package itself (mliport/pyproject.toml).
  - package-ecosystem: pip
    directory: "/mliport"
    schedule:
      interval: weekly
    open-pull-requests-limit: 10
